import styled from 'styled-components';

export const SearchBarStyled = styled.div`
    width: 100%;
    text-align: center;
    .custom-InputSearch {
        width: 60%; /* 원하는 너비로 수정 */
        max-width: 600px; /* 최대 너비를 설정하고 싶다면 */
        background-color: #f0f0f0;
    }
`;
